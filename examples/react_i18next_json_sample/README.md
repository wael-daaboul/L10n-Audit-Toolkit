# React / Next.js JSON Sample

Reserved for a minimal React / Next.js sample that demonstrates:
- JSON locale files under a profile-supported directory such as `locales/`, `public/locales/`, or `src/locales/`
- `t('key')` and `i18n.t('key')` usage detection
