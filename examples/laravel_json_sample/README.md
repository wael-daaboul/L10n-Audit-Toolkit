# Laravel JSON Sample

Reserved for a minimal Laravel JSON translation sample that demonstrates:
- `resources/lang/*.json`
- `app/`, `resources/views/`, `routes/`
- `__('key')`, `@lang('key')`, and `trans('key')` usage detection

Laravel PHP array translation files are intentionally out of scope for this phase.
